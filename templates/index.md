# Template registry

One row per template. The deck generator lists these and lets the user pick;
`modulabs-red` is the default when the user doesn't specify one.
Each `<id>` is a folder under `templates/` containing `design.md` + `template.html` + `assets/`.

| id | name | look | logo asset |
|---|---|---|---|
| `modulabs-red` | 모두의연구소 (Modulabs) | Coral full-bleed cover/section + light content slides, red+gray header rule, brand-recolored diagrams | `assets/modulabs-logo-white.png` |

<!-- Add a new row here whenever you scaffold a template under templates/<id>/ (see SKILL.md → "Add a new template"). -->
